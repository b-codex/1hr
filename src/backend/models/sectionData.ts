export interface SectionData {
    id?: string;
    timestamp: string;
    sectionID: string
    name: string
    startDate: string
    endDate: string
    relatedDept: string
    active: "Yes" | "No";
}